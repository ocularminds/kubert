Kubert is running as a native sidecar in Deployment {{ include "kubert.fullname" . }}.

Inspect the sidecar logs:
  kubectl logs -n {{ .Release.Namespace }} deployment/{{ include "kubert.fullname" . }} -c kubert

Check the managed image:
  kubectl get deployment -n {{ .Release.Namespace }} {{ include "kubert.fullname" . }} \
    -o jsonpath='{.spec.template.spec.containers[?(@.name=="{{ .Values.workload.containerName }}")].image}'

{{- if .Values.kubert.dryRun }}
Dry-run mode is enabled; Kubert will report updates without changing the Deployment.
{{- end }}
